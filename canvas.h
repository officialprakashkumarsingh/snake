```markdown
## Changelog
- 2024-06-XX – Clean slate  
  - Emptied all source files (`canvas.js`, `ctx.js`, `index.html`, `style.css`, `script.js`).  
  - Renamed mis-named files to their correct extensions (`.h` → `.js`, `.c` → `.js`, `.h` → `.html`, `.c` → `.css`).  
  - Repository now contains only empty files ready for fresh implementation.
```